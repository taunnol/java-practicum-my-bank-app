apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-front-ui-secret
  labels:
    app: front-ui
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
type: Opaque
data:
  SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_FRONT_UI_CLIENT_SECRET: {{ .Values.security.oauth2.clientSecret | b64enc | quote }}
